"""Shared constants used across the knowledge extraction system."""

# Default embedding model for semantic search and fact/message embeddings
DEFAULT_EMBEDDING_MODEL = "BAAI/bge-large-en-v1.5"

# Vector dimensions for the embedding model (must match the model's output)
EMBEDDING_VECTOR_DIMENSIONS = 1024
